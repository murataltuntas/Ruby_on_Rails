<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nick %><br>
    <%= f.text_field :nick %>
  </div>
  <div class="field">
    <%= f.label :bölüm %><br>
    <%= f.select :departmant, [ 'Bilgisayar','Elektronik','Malzeme','Matematik' , 'Mimar', 'İsletme' , 'Fizik' , 'Genetik'], :prompt => 'Select One' %>  </div>
  <div class="field">
    <%= f.label :sifre %><br>
    <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= f.label :sifreTekrar %><br>
    <%= f.password_field :password_confirmation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
